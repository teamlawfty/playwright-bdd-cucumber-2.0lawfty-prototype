{
  "compilerOptions": {
    "target": "es6", // Specify ECMAScript target version
    "module": "commonjs", // Specify module code generation
    "strict": true, // Enable all strict type-checking options
    "esModuleInterop": true, // Enables emit interoperability between CommonJS and ES Modules
    "skipLibCheck": true, // Skip type checking of declaration files
    "forceConsistentCasingInFileNames": true, // Disallow inconsistently-cased references to the same file
    "lib": ["es6", "dom"], // Include DOM types for browser compatibility
    "types": ["node", "jest"], // Include necessary type definitions
    "moduleResolution": "node", // Use Node module resolution strategy
    "resolveJsonModule": true // Include JSON modules support
  },
  "include": ["**/*.ts"], // Include all TypeScript files in the project
  "exclude": ["node_modules"] // Exclude node_modules from compilation
}
